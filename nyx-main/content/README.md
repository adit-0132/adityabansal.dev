# Writing content

Projects live in `content/projects/<slug>.svx`. The filename (minus extension) is the URL slug.

See `content/projects/_template.svx` for the frontmatter schema and a starter body.

## Preview

```
bun run dev
```

Drafts (`published = false`) show up locally only.
